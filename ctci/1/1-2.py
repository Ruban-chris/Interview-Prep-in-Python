# Given two strings, write a method to decide if one is a permutation of the 
# the other

# check both strings length are equal else false

sort both strings and compare both character by character
that is nlogn


